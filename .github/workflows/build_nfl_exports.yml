name: Build and commit NFL export files

on:
  workflow_dispatch:  # manually triggered
  repository_dispatch:
    types: [crosswalk-updated]
  push:
    branches:
      - main
    paths:
      - "schema/leagues/nfl/**"

# MLB and NFL builds both fire on `crosswalk-updated` and both push to main,
# so serialise them rather than racing on the push.
concurrency:
  group: registry-exports
  cancel-in-progress: false

jobs:
  build_nfl_exports:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Clone prism-crosswalk repo
        run: |
          git clone https://github.com/statsvine/prism-crosswalk.git crosswalk

      - name: Clone prism-tools repo
        run: |
          git clone https://github.com/statsvine/prism-tools.git tools

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.x'

      - name: pip install dependencies
        run: |
          pip install -r tools/requirements.txt

      - name: Run NFL build script
        run: |
          python3 tools/registry/nfl/build_players_registry.py crosswalk/exports/nfl/players/full/players.json exports/nfl --registry-file schema/leagues/nfl/players.yaml

      - name: Commit export files
        run: |
          git config --global user.name "PRISM Build Bot"
          git config --global user.email "statvine+prism@gmail.com"
          git add exports/
          git commit -m "Automated build: Update NFL output files" || echo "No changes to commit"
          git pull --rebase origin main
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
